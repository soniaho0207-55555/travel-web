# v2.6.1 Hotfix 派单草稿

> **用途**：PMO 把 v2.6 未兑付的 PM 内容欠账一次补齐。这是 Explorer 起草的派单规格，PMO 据此转进 `workflow/backlog.md` 并分派。
> **背景**：见 [workflow/v2.6-post-mortem.md](v2.6-post-mortem.md)
> **决策**：CEO 2026-04-16 拍板 A + B，本文件是 A 的具体派单。

---

## 一、本 hotfix 要做的事（一句话）

补齐 v2.6 承诺但未交的 **PM 内容**（whyVisit × 53 + hookShort × 53 + 购票深链 × ~49），然后 Dev-H5 粘合、QA、PMO 推。**不新增需求、不改 PRD 规格**。

---

## 二、任务分派（建议）

PMO 转进 backlog 时建议用这个骨架：

### v2.6.1-01 · P0 · PM · whyVisit 全量 53 条

- **PRD 锚点**：附录 H · H-01
- **交付路径**：`PM-v2.6-whyVisit/` 目录，按城市分 15 个 md 文件（`01-rome.md` … `15-machu-picchu.md`），每文件内按景点 name 分节
- **每条规格**：100–200 字 Monocle/Cereal 散文，必含 1 个具象锚点 + 1 个跨文明参照，禁常识句
- **PM 自检清单**（4 条全满才算交付）：锚点 ✓ / 跨文明参照 ✓ / 不与 desc/tips 重复 ✓ / 无"世界文化遗产""历史悠久"等常识填充 ✓
- **工作量估计**：~13h 纯写作，PM 建议分 2–3 个工作日

### v2.6.1-02 · P1 · PM · hookShort 全量 53 条

- **PRD 锚点**：附录 H · H-08
- **交付路径**：追加在 `PM-v2.6-whyVisit/xx-city.md` 同一文件里（每条景点 whyVisit 上方加 hookShort 段）
- **每条规格**：40 字以内一句话钩子，用于介绍 Tab 顶部
- **工作量**：~2h（顺着 whyVisit 一起写）

### v2.6.1-03 · P0 · PM · 购票死链修复（首批 15 P0 景点）

- **PRD 锚点**：附录 H · H-04 + H-13
- **输入**：`audit-links.csv`（v2.6 Dev 交的审计结果，红色状态码行）
- **交付**：
  - 每条死链给出处理方案：A 换深链 / B 改为 `searchHint` 文案（H-13 兜底 UI）/ C 整条渠道删除
  - 首批 15 个 P0 景点（Tier A 优先）必须全部闭合
  - 其余 ~34 景点可进 v2.6.2，本 hotfix 不强求
- **交付路径**：`PM-v2.6-whyVisit/tickets-fix.md` 单独一张处理表（PM 写，Dev 据此改 `js/data.js`）

### v2.6.1-04 · P0 · Dev-H5 · 粘合

- **依赖**：01 + 02 + 03 全部 PM 标 DONE 后开工（不允许部分粘合）
- **工作**：
  - 按 `PM-v2.6-whyVisit/` 15 个城市文件逐一 copy-paste 进 `js/data.js`
  - 按 `tickets-fix.md` 表改 ticket.channels 条目（删/改/加 searchHint）
  - 粘合后做一次 `grep -c "whyVisit" js/data.js` 自检，必须 ≥53
- **工作量估计**：~3h（机械粘合）

### v2.6.1-05 · P0 · QA · 三端验收

- **依赖**：04 标 DONE
- **重点**：
  - 抽查 10 景点介绍 Tab 是否真的有 whyVisit 散文（不是 fallback 空行）
  - 抽查 audit-links.csv 里首批 15 景点的购票链接是否 200 响应（可用 `scripts/audit-links.js` 复跑一次）
  - UX 验收评分目标 ≥ 7.5（v2.6 是 6.5，不要再跌）

### v2.6.1-06 · Explorer · 验证 Step 2.4 闸门是否能拦住复发

- **时机**：v2.6.1 完成后、v2.7 正式开跑前
- **工作**：在 worktree 跑一次 `/pipeline` 空转（或 mock 一个 PM 不交 whyVisit 的场景），验证 Step 2.4 真的会报 ❌ 并回 PM Mode A''
- **产出**：验证报告一页纸，挂 `workflow/step-2.4-validation.md`

---

## 三、执行顺序

```
PM 并行（~15h）：
  v2.6.1-01 whyVisit 53 条 ─┐
  v2.6.1-02 hookShort 53 条 ├─→ 全部 DONE → v2.6.1-04 粘合
  v2.6.1-03 tickets-fix 15 ─┘
                                      ↓
                                 v2.6.1-05 QA
                                      ↓
                                  PMO merge
                                      ↓
                            v2.6.1-06 Explorer 验闸
                                      ↓
                                 v2.7 开跑
```

---

## 四、不做的事（明确边界）

- ❌ 不改 PRD 规格（规格已在 v2.6 附录 H 定义清楚，这轮是纯兑付）
- ❌ 不新增 UX 2108 反馈里的条目（那些进 v2.7 走正常 pipeline）
- ❌ 不碰 Step 2.4 闸门的实现（那是 B 方案，已经写在 `.claude/commands/pipeline.md` 里，等下轮 pipeline 自然触发）
- ❌ Dev-H5 不允许动 `js/data.js` 以外的文件（这轮是纯 content 粘合）

---

## 五、给 CEO 看的完成判据

- [ ] `PM-v2.6-whyVisit/` 目录存在，里面 15 个城市 md 文件，总计 ≥53 个景点 section
- [ ] `grep -c "whyVisit" js/data.js` ≥ 53
- [ ] `grep -c "hookShort" js/data.js` ≥ 53
- [ ] `audit-links.csv` 首批 15 P0 景点行全部状态 200 或改为 searchHint
- [ ] QA 三端全 PASS
- [ ] UX 复审评分 ≥ 7.5

全满 → v2.6.1 合 main，解除 v2.6 欠账。

---

## 六、备注

这张派单不在正式 `/pipeline` 流程里跑——**因为 Step 2.4 闸门还没被验证能拦住复发**。建议 PMO 手工分派（直接 Task 调 pm / dev-h5 / qa subagent），跑完这一轮 hotfix 再恢复正常 pipeline（那时 Step 2.4 已上线）。

换句话说：**这次 hotfix 是"手动补流水线欠的债"，下一次（v2.7 起）所有"内容欠账"应该被 Step 2.4 在闸门 ② 前就拦下来**。
